Start the Vite dev server for this project.

```bash
npm run dev
```

The app runs at http://localhost:5173. Run the command in the background and confirm the server is up by checking for the "Local:" line in its output.
