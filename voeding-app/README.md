# voeding-app

Placeholder scaffold for the future nutrition-tracking app. No features implemented yet — just an empty Vite + React app, matching `workout-app`'s Vite/React versions, so the monorepo layout is ready when this app is built out.

```bash
npm install
npm run dev      # http://localhost:5173
npm run build
```
